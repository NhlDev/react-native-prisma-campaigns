export const PrismaLoad = (
  Server: String,
  Port: String,
  AppToken: String,
  CustomerId: String
) => {
  throw new Error('Platform not supported');
};

export const PrismaPlaceholder = (props: any) => {
  throw new Error('Platform not supported');
};
